{
  "name": "baseballfields",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "parcel build index.html && git add . && git commit -m 'deploy' && git subtree push --prefix dist origin gh-pages"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "d3": "^5.11.0",
    "js": "^0.1.0"
  },
  "devDependencies": {
    "cssnano": "^4.1.10"
  }
}
